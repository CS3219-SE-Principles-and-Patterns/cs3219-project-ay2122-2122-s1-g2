export enum landingEnum {
  LOGIN = 0,
  REGISTER = 1,
  FORGET_PASSWORD = 2,
}
export enum gameState {
  DEFAULT = 0,
  FINDING_OPPPONENT = 1,
  OPPONENT_FOUND = 2,
  IN_PROGRESS = 3,
  WAITING_FOR_FINISH = 4,
  FINISH = 5
}
